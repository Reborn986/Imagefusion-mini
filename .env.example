# Copy to .env.local, edit paths, then run: set -a; source .env.local; set +a
# Never commit .env.local or any access token.

BASE_MODEL=models/Lumina-mGPT-2.0-Omni
INIT_CKPT=models/imagefusion-sft-epoch4
QWEN_COT_MODEL=models/Qwen3-VL-32B-Thinking
QWEN_REWARD_MODEL=models/Qwen3-VL-8B-Instruct

PYTHON_BIN=.venv-main/bin/python
TORCHRUN=.venv-main/bin/torchrun
REWARD_PYTHON_BIN=.venv-qwen/bin/python

CUDA_VISIBLE_DEVICES=0,1,2,3
POLICY_GPUS=1,2,3
REWARD_GPU=0
NPROC=3
